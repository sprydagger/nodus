[project]
name = "nodus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["networkx>=3.6.1", "numpy>=2.4.6", "pytest>=9.0.3"]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "ruff>=0.15.14",
]
